<div class="panel panel-primary">
  <div class="panel-heading"><%= @product.model %></div>

    <div class="panel-body">

      <p><strong>Brand:</strong> <%= @product.brand %></p>
      <p><strong>Price:</strong> €<%= @product.price %></p>
      <p><strong>Category:</strong> <%= @product.category %></p>
      <p><strong>Gender:</strong> <%= @product.gender %></p>
      <p><strong>Frame:</strong> <%= @product.frame %> centimers</p>

      <div class="photos">
          <% @product.photos.each do |photo| %>
          <%= image_tag photo.image.medium %>
          <% end %>
      </div>

    </div>
  <div class="label-success">
    <%= link_to "Add to my cart!", new_lineitem_path(:product_id => @product.id), class: "btn btn-default" %>
  </div>
</div>

<%= link_to "Back", products_path, class: "btn btn-default"%>
